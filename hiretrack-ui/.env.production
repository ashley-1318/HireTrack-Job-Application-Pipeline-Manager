# Firebase deployment - relative API works for JSON.
# Direct function URL used for multipart uploads to avoid boundary truncation via Hosting rewrite.
VITE_API_BASE_URL=
# Use relative API path so requests go through Hosting rewrite (same-origin) which avoids extra preflight and CORS issues.
VITE_FUNCTION_URL=
